extends layout/app

block contenido 

        section(class='py-5')
            h2(class='text-center text-4xl font-extrabold py-10')= title
            if propiedades.length
                div(class='grid grid-cols-1 lg:grid-cols-3 gap-5 mt-14')
                    each propiedad in propiedades 
                        include ./templates/card
            else 
                p(class='text-center text-gray-800 text-xl py-10 font-bold') No Hay Resultados